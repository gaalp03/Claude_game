{
  "name": "ghost-loop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GHOST LOOP - minimalist puzzle-platformer where your past attempts become ghosts.",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host 0.0.0.0 --port 5174 --strictPort",
    "build": "vite build",
    "package": "node scripts/package.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "verify": "node scripts/verify-levels.mjs",
    "covers": "node scripts/make-covers.mjs"
  },
  "dependencies": {
    "@fontsource/exo-2": "^5.3.0",
    "@fontsource/orbitron": "^5.3.0",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "adm-zip": "^0.5.18",
    "playwright": "^1.56.1",
    "vite": "^7.3.6",
    "vitest": "^3.2.7"
  }
}
